node_modules
dist
*.local
.vite

# TypeScript's incremental build cache, and Playwright's run output: both are
# regenerated on demand and neither means anything on another machine.
*.tsbuildinfo
test-results
playwright-report
